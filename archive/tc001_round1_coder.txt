--- Reasoning ---
We are given a task to set up prerequisites for TC-001 (Deck Submission Flow).
 The steps are:
  1. Ensure there is a tournament with deck list requirement and open registration.
  2. Ensure test user is logged in and not registered for the tournament.
  3. Verify initial 'No Deck Submitted' or similar badge/state on tournament page.

 We are to provide code/commands (SQL or browser actions) to achieve this.

 Since we are in a development/testing environment, we can use SQL to set up the database state.
 Alternatively, we might use the application's UI, but the task asks for code/commands, so we'll focus on SQL.

 Assumptions:
  - We have access to the Supabase database for the TCG Manager.
  - We are using the same database schema as in the provided code.

 Steps in detail:

 1. Create a tournament with:
    - `requires_deck_list` set to true.
    - `registration_status` set to 'open' (or similar, depending on schema).

 2. Ensure a test user exists and is logged in. Since we cannot control the browser's login state via SQL, we assume we have a test user and we can log in via the UI. However, we can set up the test user in the database if needed.

 3. The test user should not be registered for the tournament. So, we must ensure there is no entry in `tournament_players` for this user and tournament.

 4. Verify that on the tournament page, the user sees a 'No Deck Submitted' badge. This is a UI state that we can check by looking at the data. Specifically, we need to check that the user has no deck list for the tournament.

 Let's break down the SQL commands:

 Step 1: Create a tournament (if one doesn't exist) with the required attributes.

 We'll need to insert into the `tournaments` table. We also need to set the `organizer_id` or `organizer_popid`? We can set it to the test user if we want the test user to be the organizer, but the test is about submitting a deck as a player. Alternatively, we can create a tournament organized by someone else.

 Since the test is about a player submitting a deck, we can create a tournament organized by another user (or the same user, but then they would be the organizer and might have different UI). Let's assume we want the test user to be a player, not the organizer.

 We can create a tournament with a known organizer (maybe we have an admin user). Alternatively, we can set the tournament's `organizer_id` to a different user.

 However, looking at the code, the tournament page does not require the user to be the organizer to register. So we can create a tournament with any organizer.

 Let's assume we have an organizer user with id `organizer_user_id` (we can get this from the `profiles` table or `auth.users`). We'll create a tournament that is open and requires a deck list.

 We also need to set the `deck_list_submission_deadline` to a future date so that the submission is open.

 Step 2: We need a test user. We can create one in the `auth.users` and `profiles` table if it doesn't exist. However, note that the test user must be logged in. We cannot simulate login via SQL, so we assume we have a test user and we can log in via the UI.

 Step 3: Ensure the test user is not registered for the tournament. We check the `tournament_players` table.

 Step 4: Verify the state. We can check that there is no entry in the `deck_lists` table for the test user and tournament.

 Since the task is to set up the state, we will provide SQL to set up the database accordingly.

 Let's write SQL commands:

 1. Create a tournament (if not exists) with the following attributes:

    We'll assume we have an organizer user (with id `organizer_user_id`). We can get one by selecting an existing user with the role of 'organizer' or 'admin'. Alternatively, we can create one if necessary.

    We'll create a tournament with:
      - id: we can generate a UUID or use a known one for testing.
      - name: 'Test Tournament for Deck Submission'
      - requires_deck_list: true
      - deck_list_submission_deadline: a future timestamp, e.g., NOW() + INTERVAL '1 week'
      - registration_status: 'open' (or whatever the schema expects, might be a boolean `registrations_open`)

    Looking at the provided code, the tournament page uses `tournament_players` for registration, so we assume the tournament is open for registration.

 2. We assume the test user exists and we know their `pokemon_player_id` (which is stored in the `profiles` table). We'll need to use that `pokemon_player_id` for registration and deck list.

 3. We must not have a row in `tournament_players` for the test user and this tournament.

 4. We must not have a row in `deck_lists` for the test user and this tournament.

 Since we are setting up for a test, we want to clean up any existing registration and deck list for this user and tournament.

 We'll write SQL that:

  - Creates a tournament (if not exists) with the desired properties.
  - Deletes any existing registration and deck list for the test user in that tournament.

 However, note: the test user might be already registered in other tournaments, so we only clean up for this tournament.

 Let's assume we have a test user with `pokemon_player_id` = 'TEST123'.

 We'll create a tournament with a known ID for easy cleanup.

 We'll use:

   Tournament ID: '00000000-0000-0000-0000-000000000001' (for example)

 Steps:

  1. Insert into tournaments if not exists.

  2. Delete from tournament_players for the test user and this tournament (to ensure they are not registered).

  3. Delete from deck_lists for the test user and this tournament.

  Then, when the test user goes to the tournament page, they should see the option to register and then submit a deck.

  However, note: the tournament page checks for `myRegistrationStatus` and `deckList` from the database. If the user is not registered, then `myRegistrationStatus` will be null, and they will see a button to register. After registration, they can submit a deck.

  But the test TC-001 is about submitting a deck list for a tournament they are already registered for? Actually, the task says: "Register for a tournament (requires deck lists), paste valid PTCG Live export, validate, and submit."

  So the user must first register. However, the task for this round is only Phase 1: Setup & Prereqs. So we just need to set up the tournament and the user in a state where they can register and then submit a deck.

  Therefore, we want:
    - The tournament exists, requires a deck list, and is open for registration.
    - The user is logged in and not registered for the tournament (so they can register).

  Then, in the test, they will register and then submit a deck.

  So our setup SQL should:

    - Create the tournament (if not exists) with the correct attributes.
    - Ensure the user is not registered (delete any existing registration and deck list).

  We'll write the SQL accordingly.

  However, note: the tournament might have a maximum number of players? We assume it does not, or we set it to a high number.

  Also, the tournament might have a start time and end time? We don't see that in the code, but we can set them to future dates.

  Let's write the SQL:

  We assume the following columns in the `tournaments` table (based on the code):
      id, name, organizer_id, organizer_popid, requires_deck_list, deck_list_submission_deadline, registration_status (or registrations_open), etc.

  We'll set:
      id: '00000000-0000-0000-0000-000000000001'
      name: 'Test Tournament for Deck Submission TC-001'
      organizer_id: we need an existing user id. Let's get one from the profiles table where role is 'organizer' or 'admin'. If none, we might create one.
      requires_deck_list: true
      deck_list_submission_deadline: NOW() + INTERVAL '7 days'
      registrations_open: true (or whatever the column is, maybe 'registration_status' with value 'open')

  We'll assume the table has a `registrations_open` boolean column. If it doesn't, we adjust accordingly.

  Since the provided code does not show the exact schema, we have to make assumptions.

  Alternatively, we can look at the `tournament-view` component to see what it expects. The tournament object is passed with `requires_deck_list` and `deck_list_submission_deadline`. The registration status is fetched from `tournament_players` for the user.

  We'll create a tournament that is open for registration by setting the `registrations_open` to true (if the column exists). If not, we might rely on the `registration_status` in `tournament_players`? Actually, the tournament might have a `registration_open` boolean.

  Let's assume the `tournaments` table has a `registrations_open` column. If it doesn't, we might need to adjust.

  We'll write the SQL for the tournament creation and then the cleanup for the test user.

  We'll also assume the test user's `pokemon_player_id` is known and is 'TEST123'.

  Here is the SQL:

  Note: We use a known tournament ID so we can refer to it in the cleanup.

  Step 1: Create the tournament if it doesn't exist.

  Step 2: Remove any existing registration and deck list for the test user for this tournament.

  We'll write it as a transaction.

  However, note: the tournament might be created by an organizer. We need to set the organizer_id. We can get an organizer by:

      SELECT id FROM auth.users 
      WHERE id IN (SELECT id FROM profiles WHERE role = 'organizer' OR role = 'admin') 
      LIMIT 1

  If there is no organizer, we can create one. But that might be out of scope. Let's assume there is at least one user with role 'organizer' or 'admin'.

  Alternatively, we can set the organizer_id to a specific user (maybe the test user, but then they would be the organizer). We want the test user to be a player, so we set the organizer to a different user.

  We'll do:

      WITH organizer AS (
          SELECT id FROM auth.users 
          WHERE id IN (SELECT id FROM profiles WHERE role = 'organizer' OR role = 'admin') 
          LIMIT 1
      )
      INSERT INTO tournaments (id, name, organizer_id, requires_deck_list, deck_list_submission_deadline, registrations_open, ... other required columns ...)
      SELECT 
          '00000000-0000-0000-0000-000000000001', 
          'Test Tournament for Deck Submission TC-001', 
          organizer.id, 
          true, 
          NOW() + INTERVAL '7 days', 
          true,
          ... other default values ...
      FROM organizer
      ON CONFLICT (id) DO NOTHING;

  But note: the `tournaments` table might have other required columns (like `format`, `date`, etc.). We must provide values for all non-nullable columns.

  We don't have the full schema, so we must adjust accordingly.

  Given the complexity and unknown schema, it might be better to use a tournament that already exists and meets the criteria, or to create one via the UI and then use its ID.

  Alternatively, we can provide a set of SQL commands that set up the tournament by updating an existing tournament to meet the criteria.

  Since the task is for testing, we can also use a known tournament ID that we create via the UI and then hard-code in the test.

  Given the constraints, I propose:

  Option 1: Use an existing tournament that meets the criteria and clean up the test user's registration and deck list for that tournament.

  Option 2: Create a tournament via the UI and then use its ID in the test.

  But the task asks for code/commands (SQL or browser actions). So we can provide SQL that updates an existing tournament to meet the criteria.

  Let's assume we have a tournament with ID `existing_tournament_id` and we want to update it to:

      requires_deck_list = true,
      deck_list_submission_deadline = NOW() + INTERVAL '7 days',
      registrations_open = true

  Then, we delete any existing registration and deck list for the test user.

  So the SQL would be:

      UPDATE tournaments 
      SET 
          requires_deck_list = true,
          deck_list_submission_deadline = NOW() + INTERVAL '7 days',
          registrations_open = true
      WHERE id = 'existing_tournament_id';

      DELETE FROM tournament_players 
      WHERE tournament_id = 'existing_tournament_id' 
        AND player_id = 'TEST123';

      DELETE FROM deck_lists 
      WHERE tournament_id = 'existing_tournament_id' 
        AND player_id = 'TEST123';

  This is simpler and does not require creating a new tournament.

  We can then use the `existing_tournament_id` in the test.

  However, note: the tournament might have other settings (like being private, or having a maximum number of players) that could affect the test. We assume it is open and has room.

  Alternatively, we can create a tournament via the UI and then use its ID in the test. Then we don't need to worry about the schema.

  Given the time, I think the update approach is safer because we don't need to know all the required columns.

  But we must ensure the tournament exists. We can use a tournament that we know exists, or we can create one via the UI and then note its ID.

  Since this is for a test, we can use a fixed tournament ID that we create via the UI once and then reuse.

  Let's assume we have a tournament with ID: `test_tournament_id` (which we create via the UI and then use in the test).

  Then, the setup SQL would be:

      UPDATE tournaments 
      SET 
          requires_deck_list = true,
          deck_list_submission_deadline = NOW() + INTERVAL '7 days',
          registrations_open = true
      WHERE id = 'test_tournament_id';

      DELETE FROM tournament_players 
      WHERE tournament_id = 'test_tournament_id' 
        AND player_id = 'TEST123';

      DELETE FROM deck_lists 
      WHERE tournament_id = 'test_tournament_id' 
        AND player_id = 'TEST123';

  We assume the test user's `pokemon_player_id` is 'TEST123'.

  If we don't know the test user's `pokemon_player_id`, we can get it from the profiles table:

      SELECT pokemon_player_id FROM profiles WHERE id = 'test_user_auth_id'

  But we might not know the auth id. Alternatively, we can use the email:

      SELECT pokemon_player_id FROM profiles 
      WHERE id = (SELECT id FROM auth.users WHERE email = 'testuser@example.com')

  So, let's assume we know the test user's email, which is 'testuser@example.com'.

  Then, we can do:

      WITH test_user AS (
          SELECT pokemon_player_id 
          FROM profiles 
          WHERE id = (SELECT id FROM auth.users WHERE email = 'testuser@example.com')
      )
      DELETE FROM tournament_players 
      WHERE tournament_id = 'test_tournament_id' 
        AND player_id = (SELECT pokemon_player_id FROM test_user);

      Similarly for deck_lists.

  We'll write the SQL accordingly.

  However, note: the `tournament_players` table uses `player_id` which is the `pokemon_player_id` (from the code in page.tsx, we see it uses `profile.pokemon_player_id` to query `tournament_players`).

  So, we assume the test user has a `pokemon_player_id` set in their profile.

  Let's write the full SQL:

      -- Update the tournament to require deck list and open registration
      UPDATE tournaments 
      SET 
          requires_deck_list = true,
          deck_list_submission_deadline = NOW() + INTERVAL '7 days',
          registrations_open = true
      WHERE id = 'test_tournament_id';

      -- Remove any existing registration for the test user
      DELETE FROM tournament_players 
      WHERE tournament_id = 'test_tournament_id' 
        AND player_id = (
            SELECT pokemon_player_id 
            FROM profiles 
            WHERE id = (SELECT id FROM auth.users WHERE email = 'testuser@example.com')
        );

      -- Remove any existing deck list for the test user
      DELETE FROM deck_lists 
      WHERE tournament_id = 'test_tournament_id' 
        AND player_id = (
            SELECT pokemon_player_id 
            FROM profiles 
            WHERE id = (SELECT id FROM auth.users WHERE email = 'testuser@example.com')
        );

  We assume the tournament with ID 'test_tournament_id' exists.

  If we don't have a tournament, we must create one. We can do:

      INSERT INTO tournaments (id, name, organizer_id, requires_deck_list, deck_list_submission_deadline, registrations_open, ...)
      VALUES (...);

  But again, we don't know the full schema.

  Alternatively, we can use the UI to create a tournament and then use its ID.

  Given the above, I think the best approach for the task is to provide:

  1. SQL to update an existing tournament to meet the criteria (if we are allowed to use an existing tournament).
  2. SQL to clean up the test user's registration and deck list for that tournament.

  And then, for the browser actions, we can log in as the test user and navigate to the tournament page.

  Since the task says "Provide the code/commands (SQL or browser actions)", we can provide both.

  However, note: the task is only for setup and prerequisites. So we don't need to do the actual test steps.

  We'll provide:

      SQL commands to set up the tournament and clean up the test user's state.

      Plus, a note to log in as the test user and go to the tournament page.

  But note: the test user must be logged in. We cannot do that via SQL.

  So, we assume the test user is already logged in, or we provide the steps to log in via the UI.

  Let's write the SQL commands and then the browser actions.

  We assume the following:

      Tournament ID: 'test_tournament_id' (to be replaced with a real tournament ID)
      Test user email: 'testuser@example.com'

  If we don't have a tournament, we can create one via the UI and then note its ID.

  Alternatively, we can create a tournament via SQL if we know the schema. Since we don't, we'll assume we have a tournament.

  We'll write the SQL for updating and cleaning up.

  We'll also write the steps for the browser:

      1. Log in as testuser@example.com (password: ...)
      2. Navigate to the tournament page: /tournament/test_tournament_id

  Then, the user should see the tournament with an option to register (if not already registered) and then, after registration, the deck submission flow.

  However, note: the test requires that the user is not registered. Our SQL ensures that.

  Now, the verification step: We need to verify the initial 'No Deck Submitted' badge.

  We can check the database to ensure there is no deck list, but the badge is a UI element. We can only verify that by looking at the page.

  So, for the verification step, we can note that we should see the 'No Deck Submitted' badge on the tournament page.

  We'll provide the SQL and then the steps to log in and navigate.

  Let's write the code/commands.

  Note: The SQL might need to be adjusted based on the actual database schema.

  We assume the following tables: tournaments, tournament_players, deck_lists, profiles, auth.users.

  We assume the columns: 
      tournaments: id, requires_deck_list, deck_list_submission_deadline, registrations_open
      tournament_players: tournament_id, player_id (pokemon_player_id)
      deck_lists: tournament_id, player_id (pokemon_player_id)
      profiles: id (same as auth.users.id), pokemon_player_id
      auth.users: id, email

  We'll write the SQL accordingly.

  If the table or column names are different, adjust accordingly.

  Let's write the SQL:

--- Answer ---
Based on the context, I'll provide SQL commands to set up the prerequisites for TC-001. This assumes you have access to the Supabase database.

## SQL Commands for Setup:

```sql
-- 1. Create/ensure a tournament with deck list requirement and open registration
INSERT INTO tournaments (
  id,
  name,
  description,
  format,
  location,
  start_date,
  requires_deck_list,
  deck_list_submission_deadline,
  registrations_open,
  organizer_id
) VALUES (
  'tc-001-test-tournament', -- Use a specific ID for easy reference
  'TC-001 Test Tournament',
  'Test tournament for deck submission flow verification',
  'Standard',
  'Online',
  NOW() + INTERVAL '3 days', -- Start in 3 days
  TRUE, -- Requires deck list
  NOW() + INTERVAL '1 day', -- Deadline in 1 day
  TRUE, -- Registrations open
  'organizer-user-id' -- Replace with actual organizer user ID
) ON CONFLICT (id) DO UPDATE SET
  requires_deck_list = TRUE,
  deck_list_submission_deadline = NOW() + INTERVAL '1 day',
  registrations_open = TRUE;

-- 2. Ensure test user exists and has a pokemon_player_id in profile
-- First, ensure the test user has a pokemon_player_id
UPDATE profiles 
SET pokemon_player_id = 'TEST123' -- Use a test ID
WHERE id = 'test-user-id'; -- Replace with test user's auth ID

-- 3. Remove any existing registration for the test user
DELETE FROM tournament_players 
WHERE tournament_id = 'tc-001-test-tournament' 
  AND player_id = 'TEST123'; -- Same pokemon_player_id

-- 4. Remove any existing deck list for the test user
DELETE FROM deck_lists 
WHERE tournament_id = 'tc-001-test-tournament' 
  AND player_id = 'TEST123';
```

## Verification Commands:

```sql
-- Verify tournament setup
SELECT 
  name,
  requires_deck_list,
  deck_list_submission_deadline,
  registrations_open
FROM tournaments 
WHERE id = 'tc-001-test-tournament';

-- Verify test user is not registered
SELECT * FROM tournament_players 
WHERE tournament_id = 'tc-001-test-tournament' 
  AND player_id = 'TEST123';

-- Verify no deck list exists
SELECT * FROM deck_lists 
WHERE tournament_id = 'tc-001-test-tournament' 
  AND player_id = 'TEST123';
```

## Browser Actions (if needed):

1. **Login as test user**:
   - Navigate to `/login`
   - Use test credentials (configured in Supabase Auth)

2. **Navigate to tournament page**:
   - Go to `/tournament/tc-001-test-tournament`

3. **Verify UI state**:
   - Check that the user sees a "Register" button (not registered)
   - If already on the roster (if `tournament_players` entry exists with status 'pending'), verify:
     - A "No Deck Submitted" badge appears
     - A "Submit Deck List" button is available

## Alternative: Use Database Seed Script

If you have a seed script, you can add:

```typescript
// In your seed script
const tournamentId = 'tc-001-test-tournament';
const testPlayerId = 'TEST123';

// Clear existing data
await supabase
  .from('deck_lists')
  .delete()
  .match({ tournament_id: tournamentId, player_id: testPlayerId });

await supabase
  .from('tournament_players')
  .delete()
  .match({ tournament_id: tournamentId, player_id: testPlayerId });

// Create tournament if not exists
await supabase
  .from('tournaments')
  .upsert({
    id: tournamentId,
    name: 'TC-001 Test Tournament',
    requires_deck_list: true,
    deck_list_submission_deadline: new Date(Date.now() + 24 * 60 * 60 * 1000).toISOString(),
    registrations_open: true,
    organizer_id: 'organizer-user-id'
  });
```

This setup ensures:
1. A tournament exists that requires deck lists with an open submission deadline
2. The test user is not registered (clean state)
3. No deck list is submitted
4. When the user visits the tournament page, they should see the initial state with "No Deck Submitted" (or similar) badge